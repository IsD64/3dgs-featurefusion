# FeatureGS (Packaged Python Version)

```shell
pip install --target . --upgrade git+https://github.com/yindaheng98/gaussian-splatting.git@master --no-deps --no-build-isolation
pip install --target . --upgrade . --no-deps
```